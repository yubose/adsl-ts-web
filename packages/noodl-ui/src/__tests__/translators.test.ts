import { expect } from 'chai'
import * as u from '@jsmanifest/utils'
import * as nt from 'noodl-types'
import * as nu from 'noodl-utils'
import * as tr from '../translators'
import { ui } from '../utils/test-utils'

describe(u.yellow(`translators`), () => {
  describe(u.italic(`image`), () => {
    xit(``, () => {
      //
    })
  })

  describe(u.italic(`path`), () => {
    xit(``, () => {
      //
    })
  })

  describe(u.italic('resource'), () => {
    xit(``, () => {
      //
    })
  })

  describe(u.italic('style'), () => {
    describe(u.italic('align'), () => {
      xit(``, () => {
        //
      })
    })

    describe(u.italic('axis'), () => {
      xit(``, () => {
        //
      })
    })

    describe(u.italic('isHidden'), () => {
      xit(``, () => {
        //
      })
    })

    describe(u.italic('shadow'), () => {
      xit(``, () => {
        //
      })
    })

    describe(u.italic('style'), () => {
      describe(u.italic('border'), () => {
        xit(``, () => {
          //
        })
      })
    })

    describe(u.italic('textAlign'), () => {
      xit(``, () => {
        //
      })
    })

    describe(u.italic('textColor'), () => {
      xit(``, () => {
        //
      })
    })
  })
})
