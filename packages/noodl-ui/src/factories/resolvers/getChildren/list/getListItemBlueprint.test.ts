import { expect } from 'chai'

describe('getListItemBlueprint', () => {
  describe('create', () => {
    it('should remove the "left" and "top"', () => {
      //
    })

    it('should switch position to "relative"', () => {
      //
    })

    it('should pass in iteratorVar, listId and list and', () => {
      //
    })
  })
})
