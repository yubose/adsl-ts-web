import React from 'react'
import * as u from '@jsmanifest/utils'
import * as nt from 'noodl-types'

function usePageObjects({ initialState }: { initialState }) {
  //
}

export default usePageObjects
