export enum Basic {
  Right = 'Right',
  Evaluate = 'Evaluate',
  If = 'If',
  Else = 'Else',
  Extend = 'Extend',
  Goto = 'Goto',
  Inherit = 'Inherit',
  Override = 'Override',
  Left = 'Left',
}

export enum Organic {
  Convolve = 'Convolve',
  Elite = 'Elite',
  Emit = 'Emit',
  Evolve = 'Evolve',
}
