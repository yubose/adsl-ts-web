export enum BasicInstructionType {
  Collect = 'Collect',
  Evaluate = 'Evaluate',
  If = 'If',
  Else = 'Else',
  Extend = 'Extend',
  Goto = 'Goto',
  Inherit = 'Inherit',
  Override = 'Override',
}

export enum OrganicInstructionType {
  Convolve = 'Convolve',
  Elite = 'Elite',
  Emit = 'Emit',
  Evolve = 'Evolve',
}
