Scan:
  pageNumber: "0"

  title: "Test Page"

  init:

  formData:

    ddd: "none"
    popShow: "none"
  apiRequest:

  components:
    - type: view
      style:
        width: 1
        height: "1"
        display: flex
        alignItems: center
        justifyContent: center
      children:
        - type: button
          contentType: scan
          text: Click
          style:
            width: 0.2
            height: "0.1"
          onClick:
            - actionType: scanCamera
              dataKey: Scan.formData.popShow


            - actionType: evalObject
              object:
                - ..formData.ddd@: =..formData.popShow.name


            # - actionType: evalObject
            #   object:
            #     - ..formData.popShow@: "444"
