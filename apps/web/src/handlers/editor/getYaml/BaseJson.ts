interface REQUIRED {
    key: string
    title: string
}

const BaseJson = {
    formData: {
        
    },
    required: new Array<REQUIRED>(),
    components: {
        
    }
}

export default BaseJson