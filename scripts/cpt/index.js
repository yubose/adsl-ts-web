const u = require('@jsmanifest/utils')
const {
  lf,
  clear,
  each,
  getKeys,
  getKeyCount,
  getItem,
  setItem,
  supportsIndexedDB,
  supportsWebSQL,
} = require('./lf')

window.addEventListener('load', function (evt) {
  console.log(`[LOADED]`)
  //
})
