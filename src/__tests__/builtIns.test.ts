describe('builtIns', () => {
  describe('redraw', () => {
    xit('should target the components that have the viewTag if it is provided', () => {})

    xit('should fall back to targeting the component that called redraw', () => {
      //
    })
  })
})
